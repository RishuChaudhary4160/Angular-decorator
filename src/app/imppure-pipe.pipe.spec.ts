import { ImppurePipePipe } from './imppure-pipe.pipe';

describe('ImppurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImppurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
